name: "Masking policy with non-existent consumer group - manual review"
description: "Masking policy referencing a consumer group that doesn't exist in the repository should require manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "not found in repository"

mr_metadata:
  title: "Add masking policy with missing consumer"
  author: "testuser"
  source_branch: "feature/add-masking-policy"
  target_branch: "main"


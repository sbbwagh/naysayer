# Invalid masking policy - uses hyphens instead of underscores
kind: MaskingPolicy
name: analytics-pii-string-policy
data_product: analytics
datatype: string
mask: "==MASKED=="
cases:
  - strategy: UNMASKED
    consumers:
      - kind: consumer_group
        name: dataverse-source-analytics


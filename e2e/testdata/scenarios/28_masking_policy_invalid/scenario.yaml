name: "Invalid masking policy - wrong naming convention"
description: "Masking policy with invalid naming convention requires manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "Masking policy validation failed"

mr_metadata:
  title: "Add invalid masking policy"
  author: "testuser"
  source_branch: "feature/add-invalid-masking"
  target_branch: "main"


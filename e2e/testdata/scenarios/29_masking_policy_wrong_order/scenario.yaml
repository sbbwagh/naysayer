name: "Invalid masking policy - wrong strategy order"
description: "String masking policy with HASH_SHA1 before UNMASKED requires manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "⚠️ **Manual review required**"
    - "Masking policy validation failed"

mr_metadata:
  title: "Add masking policy with wrong strategy order"
  author: "testuser"
  source_branch: "feature/add-wrong-order-masking"
  target_branch: "main"


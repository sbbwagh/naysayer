# Masking policy that will be deleted
kind: MaskingPolicy
name: analytics_pii_string_policy
data_product: analytics
datatype: string
mask: "==MASKED=="
cases:
  - strategy: UNMASKED
    consumers:
      - kind: consumer_group
        name: dataverse-source-analytics

